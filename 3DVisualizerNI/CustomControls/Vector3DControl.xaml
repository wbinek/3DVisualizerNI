<UserControl x:Class="_3DVisualizerNI.CustomControls.Vector3DControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_3DVisualizerNI.CustomControls"
             mc:Ignorable="d"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Name="Root">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label Content="X:"></Label>
                <xctk:DecimalUpDown Value="{Binding X, Mode=TwoWay}"></xctk:DecimalUpDown>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Y:"></Label>
                <xctk:DecimalUpDown Value="{Binding Y, Mode=TwoWay}"></xctk:DecimalUpDown>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Z:"></Label>
                <xctk:DecimalUpDown Value="{Binding Z, Mode=TwoWay}"></xctk:DecimalUpDown>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
